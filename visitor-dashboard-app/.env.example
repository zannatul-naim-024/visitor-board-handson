# Copy to .env and set values (or set env vars on EC2 / in launch template)
PORT=3000
AWS_REGION=eu-north-1
TABLE_NAME=workshop_messages

# Local development: point at DynamoDB Local (docker compose)
# DYNAMODB_ENDPOINT_URL=http://localhost:8000
